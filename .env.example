# Google Cloud Storage Configuration
GCS_PROJECT_ID=your-gcp-project-id
GCS_BUCKET_NAME=your-bucket-name
GOOGLE_APPLICATION_CREDENTIALS=credentials/googlecloud/sa.json

# API Settings (optional - defaults are fine)
# API_HOST=0.0.0.0
# API_PORT=8000
# API_RELOAD=True

# CORS Settings (optional - defaults are fine)
# CORS_ORIGINS=["http://localhost:5173", "http://localhost:5174"]

# Signed URL expiration in minutes (optional)
# SIGNED_URL_EXPIRATION_MINUTES=60
